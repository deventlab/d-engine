[package]
name = "demo"
version = "0.1.0"
edition = "2021"

[dependencies]
# d-engine = "0.1.1"
d-engine = { path = "../../../dengine" }

tokio = { version = "1.4", features = ["signal", "rt-multi-thread"] }
log = "0.4.20"

tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }
tracing-appender = "0.2"
