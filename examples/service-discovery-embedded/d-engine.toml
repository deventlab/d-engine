# Embedded Service Discovery Configuration

[cluster]
# Use a different port to avoid conflict with standalone example (50051-50053)
listen_address = "127.0.0.1:50054"

[raft.watch]
# Enable Watch feature for service discovery notifications
enabled = true
